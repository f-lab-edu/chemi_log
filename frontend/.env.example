# 백엔드 오리진. next.config.ts 의 /api rewrite 대상이다.
#
# 브라우저 번들에는 들어가지 않지만 런타임 변수도 아니다.
# rewrite 규칙은 빌드 때 .next/routes-manifest.json 에 문자열로 박히므로,
# 이 값을 바꾸면 다시 빌드해야 한다. next start 앞에 붙여도 반영되지 않는다.
BACKEND_ORIGIN=http://localhost:8080
