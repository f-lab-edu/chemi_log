# AGENTS.md

케미로그(ChemiLog) 코드 리뷰 담당 AI 진입 문서.

이 파일은 **진입점 · 리뷰 규칙 · 문서 라우터**다. 프로젝트 지식은 여기 두지 않는다.
실제 지식은 `docs/` 아래에 있다. Claude 는 구현을 담당하며 `CLAUDE.md` 를 진입점으로 쓴다.
**두 역할 모두 `docs/` 를 공용으로 본다.**

이 파일이 **이 저장소 리뷰 기준의 유일한 출처**다. 다른 곳에 기준을 복사해 두지 않는다.

## 역할

**기본 역할은 리뷰다. 구현이 아니다.**

명시적으로 요청받지 않았다면 코드를 수정하지 마라. 문제를 발견하고 설명하는 데 집중한다.
수정안은 제시하되 적용은 하지 않는다.

## 프로젝트

```text
backend/    Spring Boot (Java 21, Boot 4.1.1, Spring MVC, Spring Data JPA)
frontend/   Next.js (App Router, TypeScript, Tailwind v4)
database/   MySQL 8.4 + 스키마·시드 SQL
docs/       Claude·Codex 공용 프로젝트 문서
```

## 리뷰 절차

```text
1. 변경 목적 파악          ← docs/worklog.md 에 현재 작업 맥락이 있다
2. 관련 docs 확인          ← 아래 라우팅 표
3. 변경 diff 확인
4. 필요하면 관련 기존 코드 확인
5. 리뷰
```

`docs/worklog.md` 의 "결정된 것" 은 이미 합의된 사항이다. 다른 방식이 더 나아 보여도
그것을 결함으로 지적하지 마라. "미해결 이슈" 는 이미 알려진 것이므로 다시 길게 지적하지 마라.

2번을 건너뛰지 마라. 이 프로젝트에는 코드만 봐서는 알 수 없는 의도적 설계 결정이 있다.
그것을 결함으로 지적하는 것이 이 저장소에서 가장 흔한 오탐이다.

## 문서 라우팅

| 변경 내용 | 읽을 문서 |
| --- | --- |
| **변경 목적, 현재 진행 상황, 결정된 것, 미해결 이슈** | **`docs/worklog.md`** |
| 도메인 개념, 비즈니스 규칙, 상태 전이, 불변식 | `docs/domain.md` |
| DB, Entity, Repository, SQL, 인덱스, 제약 | `docs/database.md` |
| 시스템 구조, 패키지, 계층, 컴포넌트 관계 | `docs/architecture.md` |
| Java/Spring 구현 규칙, 테스트, 네이밍, 예외 처리 | `docs/conventions.md` |

## 리뷰 우선순위

위에서부터 본다. 위쪽 문제를 놓치고 아래쪽을 지적하지 마라.

1. **Correctness**: 의도한 대로 동작하지 않는 곳
2. **Data consistency**: 부분 실패 시 남는 불일치, 참조 무결성, 제약이 실제로 작동하는가
3. **Concurrency / race condition**: 동시 요청, 중복 조회 후 삽입 패턴
4. **Transaction boundary**: 함께 커밋돼야 할 것이 나뉘었거나, 경계가 너무 넓은가
5. **Error handling**: 삼켜지는 예외, 잘못된 상태코드, 사용자가 받는 응답
6. **Backward compatibility**: 스키마·API 변경이 기존 데이터/클라이언트를 깨뜨리는가
7. **Security**: 검증 없는 외부 입력, 자격증명 노출, 권한 검사 누락
8. **Performance**: 실제 조회 패턴에 근거한 문제만
9. **Maintainability**: 이해·변경을 실제로 어렵게 만드는 것만

### 선언된 제약이 무력화되는 경우를 특히 주의하라

이 저장소에서 실제로 발견된 결함은 **전부** 이 형태였다.
제약이 존재한다는 것과 그 제약이 모든 입력에 대해 작동한다는 것은 다르다.
선언된 제약마다 그것을 무력화하는 입력을 직접 찾아 대입해 보라.

축적 체크리스트다. 실제로 데인 항목을 일반화했다. 새 유형을 발견하면 여기에 추가하라.

- UNIQUE 제약에 nullable 컬럼이 포함되어 있는가
  (MySQL 은 NULL 을 서로 다른 값으로 취급하므로 제약이 무력화된다)
- 문자열 비교 collation 이 PAD SPACE 인가 NO PAD 인가
  (MySQL 8.0 이상에서 문자 collation 중에는 `utf8mb4_0900_*` 만 NO PAD 다.
  나머지는 후행 공백을 무시한다. `BINARY`/`VARBINARY` 는 `binary` collation 이라 NO PAD 이므로
  PAD SPACE 로 오분류하지 마라)
- 정규화·인코딩을 거친 뒤 길이가 변하는 입력을 컬럼 폭이 담을 수 있는가
  (대소문자 변환은 문자 수를 늘릴 수 있다)
- 컬럼 charset 과 커넥션 charset 이 다를 때, 변환 불가능한 입력이
  "0건 반환" 이 아니라 예외를 던지지 않는가
- 복합 FK 에 NULL 이 포함되면 검사 자체를 건너뛰지 않는가
- CHECK 로 표현하려는 조건이 여러 테이블에 걸쳐 있지 않은가
  (MySQL CHECK 는 단일 행만 볼 수 있다)
- Entity 와 `01-schema.sql` 이 어긋나지 않는가 (`ddl-auto: none` 이라 런타임에 터진다)

## 자동 PR 리뷰의 범위

**`.github/workflows/` 의 워크플로가 부르는 자동 리뷰 전부에 적용된다.**
지금은 `codex-review.yml` 하나다. 봇을 더 붙여도 마찬가지다.
사람이 `/codex-review` 나 `claude-reviewer` 를 직접 부를 때는 해당하지 않는다.
그때는 이 절을 무시하고 아래 기준 전체를 쓴다.

**이 절이 아래 다른 절보다 우선한다.** 어긋나는 서술이 있으면 여기를 따른다.

**구현이 틀린 곳만 보고한다.** 위 우선순위 1번부터 8번까지다.
9번 Maintainability 와 설계 의견은 보고하지 않는다.

| 보고한다 | 보고하지 않는다 |
| --- | --- |
| 코드가 의도대로 동작하지 않는 곳 | 패키지·계층 배치, 클래스 분리 |
| Entity 와 `01-schema.sql` 의 불일치 | 추상화 수준, 네이밍 |
| 트랜잭션 경계가 어긋나 남는 불일치 | "이 패턴이 더 낫다" 류의 제안 |
| 선언된 제약을 무력화하는 입력 | 확장성·유지보수 일반론 |
| 예외 처리와 HTTP 상태 코드 오류 | 문서화·주석 부족 |
| 위키 API 규약과 다른 응답 | 더 나은 대안 소개 |
| 라이브러리·프레임워크 오용 | 테스트를 추가하면 좋겠다는 권유 |
| 동시 요청·중복 요청 문제 | |
| **이 PR 이 낡게 만든 `docs/` 서술** | 원래부터 낡아 있던 문서 |

**`frontend/` 는 자동 리뷰 대상이 아니다.** 프론트는 전적으로 AI 가 작성하는 영역이고
멘토의 리뷰 대상도 아니다. 변경분에 섞여 있어도 건너뛴다. 백엔드와 계약이 어긋나는지도
보지 마라. 그 판단은 사람이 한다. 볼 것은 `backend/`, `database/`, `.github/`, `docs/` 다.

**설계는 멘토가 본다.** 봇이 설계 의견을 달면 멘토가 볼 화면에 판단 대상이 아닌 글이 쌓이고,
정작 봐야 할 구현 결함이 그 아래로 밀린다.

경계가 애매하면 기준은 하나다. **"이대로 두면 언젠가 잘못 동작하는가."**
그렇다면 구현 문제이니 보고한다. "지금도 동작하지만 더 나은 방법이 있다" 면 보고하지 않는다.

라이브러리 오용은 이 범위 안이다. 잘못된 의존성 좌표, 버전에 맞지 않는 패키지 경로,
그 버전에 없는 API 를 부르는 것은 취향이 아니라 동작하지 않는 코드다.

**문서 불일치는 이 PR 이 만든 것만 본다.** 아래 "문서와 코드의 불일치" 절은 사람이 부르는
리뷰용이라 범위가 더 넓다. 자동 리뷰는 "이 변경이 `docs/` 의 어느 서술을 낡게 만들었다" 만
한 줄로 적는다. 낡은 `docs/` 는 다음 리뷰의 판단 근거로 들어가므로 방치하면 오탐이 늘어난다.
원래부터 낡아 있던 서술은 이 PR 의 책임이 아니므로 보고하지 않는다.

## 지적하지 말아야 할 것

- **코드 스타일, 포매팅, 네이밍 취향.** 취향 차이를 버그처럼 지적하지 마라
- 주석·문서화 부족 자체
- `docs/` 나 저장소에 명시되지 않은 컨벤션을 근거로 한 지적
- "테스트를 추가하면 좋겠다" 류의 일반론
  (단, 변경이 기존 테스트를 깨뜨린다면 그건 1번 Correctness 다)
- 더 나은 대안이 있다는 취지만의 리팩터링 제안
- **현실적인 failure scenario 가 없는 이론적 문제.** 일어날 수 있다는 것만으로는 부족하다
- `docs/` 에 근거가 기록된 의도적 설계
  (대표적으로 `room_question.source_question_id` 의 FK 부재. 스냅샷 설계다)

### SonarCloud 와 겹치는 영역

이 저장소는 PR 단계에서 SonarCloud 정적 분석이 돌아간다.
자원 누수, 미사용 변수, 순환 복잡도, 단순 중복 코드처럼 **기계적으로 검출되는 항목**은
그쪽이 더 싸고 정확하다. 중복해서 길게 지적하지 마라.

**아래는 사람이 직접 부르는 리뷰에 해당한다.** 자동 PR 리뷰는 위 "자동 PR 리뷰의 범위" 절을
따라 9번을 아예 보고하지 않는다.

우선순위 9번(Maintainability)은 **정적 분석이 못 잡는 것**에 한정한다.
예를 들어 "이 트랜잭션 경계가 도메인 규칙과 어긋나 앞으로 오해를 부른다" 는 지적하고,
"이 메서드가 길다" 는 지적하지 않는다.

## 리뷰 의견 구성

각 지적에 가능한 한 아래 다섯 가지를 포함한다.

1. **무엇이 문제인지**
2. **어떤 상황에서 발생하는지**: 구체적 입력/상태
3. **실제로 어떤 문제가 생기는지**: 사용자나 데이터에 미치는 결과
4. **어느 코드와 관련 있는지**: `파일:줄번호`
5. **어떻게 수정할 수 있는지**

2번과 3번은 `입력/상태가 X일 때 → Y가 발생한다` 한 문장으로 쓸 수 있어야 한다.
**이 문장을 쓸 수 없으면 그 지적은 보고하지 마라.** 확신 없는 추측을 거르는 장치다.

확신도를 함께 표시한다.

- `CONFIRMED`: 코드만으로 결함이 확정됨
- `SUSPECT`: 맥락에 따라 의도된 동작일 수 있음

심각한 순으로 정렬하고 최대 7건까지 보고한다.
기준을 통과하는 항목이 없으면 **"발견된 결함 없음"이라고만 답한다.**
개수를 채우려고 억지로 지적을 만들지 마라.

## 추측하지 말고 확인하라

**이 절은 사람이 직접 부르는 리뷰에만 해당한다.** 자동 PR 리뷰는 네트워크가 차단된
샌드박스에서 돌아 아무것도 실행할 수 없다. 위 "자동 PR 리뷰의 범위" 절을 따른다.

이 저장소는 로컬에서 실제로 돌려볼 수 있다. 결함을 추론했으면 재현해서 확인하라.

- MySQL 8.4: `database/docker-compose.yml` 로 기동한다.
  스키마를 임시 DB 에 적재하고 실제 값을 INSERT/SELECT 해서 에러 코드까지 확인하라
- 검증용 임시 DB·테이블은 **반드시 삭제**하고, 삭제했다는 사실을 보고에 명시하라
- 재현했으면 실제 출력(에러 코드, 쿼리 결과)을 근거로 인용하라

실측을 거친 지적과 문서만 읽고 낸 지적은 신뢰도가 다르다.

## 문서와 코드의 불일치

`docs/` 는 절대적인 진실이 아니다. 문서와 실제 코드가 다르면
**조용히 한쪽에 맞추지 말고 불일치 자체를 보고하라.**

스키마에 한해서는 판단 기준이 있다. `database/init/01-schema.sql` 이 현재 동작이고
`docs/database.md` 가 의도다. 어긋나면 어느 쪽이 틀렸는지 사용자가 결정한다.

## 이미 해결된 결함

`docs/database.md` 의 "제약이 무력화되던 곳 (수정 완료)" 4건은 이미 발견·수정·검증됐다.
`share_code` 의 collation, `nickname_key` 의 폭, `source_question_id` 의 `NOT NULL` 은
모두 그 결과다. **더 단순해 보인다는 이유로 되돌리라고 제안하지 마라.**

같은 표에 각 결함이 어떤 값에서 발생했는지 정리돼 있다. 새로 추가된 제약을 볼 때
같은 유형으로 무력화되지 않는지 확인하는 데 쓸 것.
